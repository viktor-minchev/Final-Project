<Window x:Class="WpfApp1.SignUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="SignUp" Height="450" Width="450"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
            <StackPanel Margin="20,20,20,10.5">
            <TextBlock Text="Sign Up" Style="{DynamicResource  MaterialDesignHeadline1TextBlock}"/>
            <Label Content="Fill in the following fields"/>
            <TextBox x:Name="txtUsername" Text="Username"/>
            <TextBox x:Name="txtFirstName" Text="First Name"/>
            <TextBox x:Name="txtLastName" Text="Last Name"/>
            <TextBox x:Name="txtEmail" Text="E-mail"/>
            <Label Content="Password:"/>
            <PasswordBox x:Name="PasswordBox"/>
            <Button x:Name="Submit" Content="Submit" Click="Submit_Click" Margin="0,30,0,0"/>
        </StackPanel>
        </Grid>
</Window>
